{
  "id": null,
  "uid": "opennms-mock",
  "title": "OpenNMS Mock Overview",
  "tags": ["scnms","opennms"],
  "timezone": "browser",
  "schemaVersion": 30,
  "version": 1,
  "refresh": "10s",
  "panels": [
    {
      "type": "stat",
      "title": "Nodes Up",
      "gridPos": {"x": 0, "y": 0, "w": 6, "h": 4},
      "id": 1,
      "datasource": {"uid": null, "type": "prometheus", "name": "${DS_PROMETHEUS}"},
      "targets": [
        {
          "expr": "sum(opennms_node_up)",
          "legendFormat": "nodes_up",
          "refId": "A"
        }
      ],
      "options": {"orientation": "auto", "colorMode": "value"}
    },
    {
      "type": "timeseries",
      "title": "Node CPU %",
      "gridPos": {"x": 6, "y": 0, "w": 12, "h": 6},
      "id": 2,
      "datasource": {"uid": null, "type": "prometheus", "name": "${DS_PROMETHEUS}"},
      "targets": [
        {"expr": "opennms_node_cpu_percent", "legendFormat": "{{node}}", "refId": "A"}
      ]
    },
    {
      "type": "table",
      "title": "Node List",
      "gridPos": {"x": 0, "y": 4, "w": 18, "h": 6},
      "id": 3,
      "datasource": {"uid": null, "type": "prometheus", "name": "${DS_PROMETHEUS}"},
      "targets": [
        {"expr": "opennms_node_up", "legendFormat": "{{node}}", "refId": "A"}
      ],
      "options": {"showHeader": true}
    }
  ],
  "templating": {"list":[]},
  "annotations": {"list":[]},
  "links": []
}
